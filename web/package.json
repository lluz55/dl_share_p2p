{
  "name": "p2pshare-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types build.ts",
    "watch": "node --experimental-strip-types build.ts --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
